# Selective inference with p-values

## Installation

```bash
mamba update -y conda mamba
mamba env create
conda activate seldom
poetry config virtualenvs.create false --local
poetry install --no-root
```